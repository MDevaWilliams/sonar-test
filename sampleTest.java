import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class SampleAppTest {
    @Test
    public void testDivideNumbers() {
        assertEquals(2, SampleApp.divideNumbers(4, 2));
    }
}
